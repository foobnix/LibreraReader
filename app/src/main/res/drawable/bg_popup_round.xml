<?xml version="1.0" encoding="utf-8"?>
<!-- The sheet every menu is drawn on: the platform's own floating colour, so a popup still
     answers to the light and dark themes, but cut to a small round instead of a square. The
     padding is vertical only - the menu measures its own width from its widest line, and
     side padding would crop the text off it. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <solid android:color="?android:attr/colorBackgroundFloating" />

    <corners android:radius="@dimen/popup_radius" />

    <padding
        android:bottom="@dimen/dv"
        android:top="@dimen/dv" />

</shape>
