<?xml version="1.0" encoding="utf-8"?>
<!-- The chooser wears the head the draging popups wear: a card cut to the same round, a bar
     across the top carrying the name in caps and the mark that closes it, and the browser
     itself filling everything under that. -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="2dip"
    android:background="?android:attr/colorBackground"
    android:elevation="4dp"
    card_view:cardCornerRadius="@dimen/radius"
    card_view:cardUseCompatPadding="true" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical" >

        <LinearLayout
            android:id="@+id/topLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="0"
            android:background="#dd4E342E"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:padding="2dip" >

            <!-- The name of the dialog, set in caps and bold, as the draging popups set theirs. -->
            <TextView
                android:id="@+id/dialogTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center_vertical"
                android:paddingLeft="10dip"
                android:singleLine="true"
                android:text="@string/choose_"
                android:textAllCaps="true"
                android:textColor="@color/white"
                android:textStyle="bold" />

            <ImageView
                android:id="@+id/closePopup"
                style="@style/buttonWhiteClose"
                android:layout_width="@dimen/wh_button"
                android:layout_height="@dimen/wh_button"
                android:layout_weight="0"
                android:contentDescription="@string/close_dialog_and_apply_changes"
                android:src="@drawable/glyphicons_599_menu_close" />
        </LinearLayout>

        <FrameLayout
            android:id="@+id/metaGenreID"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />
    </LinearLayout>

</androidx.cardview.widget.CardView>
